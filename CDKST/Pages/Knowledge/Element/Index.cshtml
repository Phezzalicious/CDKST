@page
@model CDKST.Pages_Knowledge_Element.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KnowledgeElement[0].term_identifier)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KnowledgeElement[0].cartesian_index)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KnowledgeElement[0].semiotic_index)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KnowledgeElement[0].descriptor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KnowledgeElement[0].etymology)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.KnowledgeElement) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.term_identifier)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cartesian_index)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.semiotic_index)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descriptor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.etymology)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
